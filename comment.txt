Привет) 

Есть вопрос по поводу времени 

В шаблоне кода была такая строчка кода "current_timestamp_utc = int(round(datetime.utcnow().timestamp()))"

При тестировании функционала, при записи в кафку и постргес, если передавать переменную 
current_timestamp_utc значние одно и тоже, что в самом начале запуска стрима, что спустя некоторое время
Есть предположение, что как оно один раз посчиталось при запуске стрима так таким и останется(могу быть не прав)
Поэтому при сравнении даты акции ресторана и так же при записи в кафку и постгрес сделал через "unix_timestamp(current_timestamp())"

Опять же не уверен, что так правильно, можешь подсказать. Заранее спасибо)